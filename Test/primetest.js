/**
 * Basic testing for a power of 2 using a mocha Testing
 */
const assert = require("assert");
const power = require("../Basic/Powerof2/power");
describe("positive tesing for Basic Program ", function() {
  it("the power is ", function() {
    try {
      assert.equal("power", Number);
    } catch (e) {
      console.error("not a number");
    }
  });
});
